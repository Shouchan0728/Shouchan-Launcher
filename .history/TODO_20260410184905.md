# Shouchan Launcher - TODO

## 📌 プロジェクト情報

| 項目 | 内容 |
|---|---|
| サーバーOS | Arch Linux |
| Webサーバー | nginx |
| ドメイン | srgk.ddns.net |
| ファイアウォール | UFW |
| APIサーバー場所 | `/srv/http/shouchan-api` |
| APIポート（内部） | 3000 |

---

## ✅ 完了済み（ランチャー側）

- [x] Electron + React + TypeScript + TailwindCSS プロジェクト構成
- [x] 初期設定ウィザード（Shouchanアカウント作成・Microsoft認証・Java・メモリ設定）
- [x] Microsoftログイン実装（msmc v5）
- [x] Shouchanアカウントシステム（開発者 / プレイヤー 区別）
  - プレイヤー：Microsoftログイン必須
  - 開発者：Microsoftまたはオフラインログイン可
- [x] ModPack更新通知・未更新時の起動ブロック
- [x] 開発者メニュー（ファイルアップロード・ModPack情報・ニュース管理）
- [x] Java自動インストール（Adoptium JRE 17）
- [x] ウィンドウ操作ボタンを全画面に常時表示
- [x] `npm run dev` で起動確認済み

---

## 📋 未定事項（決まり次第記入）

- [ ] 使用するMinecraftバージョン: `______`
- [ ] 使用するForgeバージョン: `______`
- [ ] 導入するMod一覧: （別途リストアップ）
- [ ] MinecraftサーバーのIPアドレス/ポート: `______`

---

## 🖥️ サーバー側でやること

### 1. パッケージのインストール

```bash
sudo pacman -Syu
sudo pacman -S nodejs npm certbot certbot-nginx
```

---

### 2. UFW ファイアウォールの設定

```bash
sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw status verbose
```

---

### 3. APIサーバーのセットアップ

```bash
sudo mkdir -p /srv/http/shouchan-api
sudo chown $USER:$USER /srv/http/shouchan-api
cd /srv/http/shouchan-api
npm init -y
npm install express cors multer
```

---

### 4. `server.js` を作成する

`/srv/http/shouchan-api/server.js` に以下の内容を丸ごとコピーする。

> ⚠️ `ADMIN_TOKEN` の値は `src/main/index.ts` の `DEV_ADMIN_TOKEN` と**必ず同じ値**にすること。

```js
const express = require('express')
const cors = require('cors')
const path = require('path')
const fs = require('fs')
const multer = require('multer')

const app = express()
app.use(cors())
app.use(express.json())

// ===== 設定 =====
const BASE_DIR = '/srv/http/shouchan-api'
const ADMIN_TOKEN = 'shouchan-admin-secret' // ← src/main/index.ts の DEV_ADMIN_TOKEN と合わせる

// ===== 公開エンドポイント =====

// ModPackファイルを静的配信
app.use('/modpack/files/download', express.static(path.join(BASE_DIR, 'modpack')))

// GET /status
app.get('/status', (req, res) => res.json({ status: 'ok' }))

// GET /modpack/info
app.get('/modpack/info', (req, res) => {
  const info = JSON.parse(fs.readFileSync(path.join(BASE_DIR, 'modpack-info.json'), 'utf8'))
  res.json(info)
})

// GET /modpack/files
app.get('/modpack/files', (req, res) => {
  const data = JSON.parse(fs.readFileSync(path.join(BASE_DIR, 'modpack-files.json'), 'utf8'))
  res.setHeader('x-modpack-version', data.version)
  res.json(data.files)
})

// GET /news
app.get('/news', (req, res) => {
  const news = JSON.parse(fs.readFileSync(path.join(BASE_DIR, 'news.json'), 'utf8'))
  res.json(news)
})

// ===== 管理者認証ミドルウェア =====
function adminAuth(req, res, next) {
  const token = req.headers['authorization']?.replace('Bearer ', '')
  if (token !== ADMIN_TOKEN) return res.status(401).json({ error: 'Unauthorized' })
  next()
}

// ===== 開発者専用エンドポイント (/admin/*) =====

// GET /admin/files - ファイル一覧
app.get('/admin/files', adminAuth, (req, res) => {
  const modpackDir = path.join(BASE_DIR, 'modpack')
  const result = []
  function walk(dir, base = '') {
    if (!fs.existsSync(dir)) return
    for (const name of fs.readdirSync(dir)) {
      const full = path.join(dir, name)
      const rel = base ? `${base}/${name}` : name
      if (fs.statSync(full).isDirectory()) {
        walk(full, rel)
      } else {
        result.push({ path: rel, size: fs.statSync(full).size })
      }
    }
  }
  walk(modpackDir)
  res.json(result)
})

// POST /admin/upload - ファイルアップロード
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    const dest = path.dirname(path.join(BASE_DIR, 'modpack', req.body.path || file.originalname))
    fs.mkdirSync(dest, { recursive: true })
    cb(null, dest)
  },
  filename: (req, file, cb) => cb(null, path.basename(req.body.path || file.originalname))
})
const upload = multer({ storage })
app.post('/admin/upload', adminAuth, upload.single('file'), (req, res) => {
  res.json({ success: true })
})

// DELETE /admin/files - ファイル削除
app.delete('/admin/files', adminAuth, (req, res) => {
  const filePath = path.join(BASE_DIR, 'modpack', req.body.path)
  if (fs.existsSync(filePath)) fs.unlinkSync(filePath)
  res.json({ success: true })
})

// PUT /admin/modpack/info - ModPack情報を更新
app.put('/admin/modpack/info', adminAuth, (req, res) => {
  fs.writeFileSync(path.join(BASE_DIR, 'modpack-info.json'), JSON.stringify(req.body, null, 2))
  res.json({ success: true })
})

// GET /admin/news - ニュース取得（管理者用）
app.get('/admin/news', adminAuth, (req, res) => {
  const news = JSON.parse(fs.readFileSync(path.join(BASE_DIR, 'news.json'), 'utf8'))
  res.json(news)
})

// PUT /admin/news - ニュース更新
app.put('/admin/news', adminAuth, (req, res) => {
  fs.writeFileSync(path.join(BASE_DIR, 'news.json'), JSON.stringify(req.body, null, 2))
  res.json({ success: true })
})

app.listen(3000, () => console.log('Shouchan API running on port 3000'))
```

---

### 5. JSONファイルを作成する

**`/srv/http/shouchan-api/modpack-info.json`**
```json
{
  "version": "v1.0.0",
  "mcVersion": "1.20.1",
  "forgeVersion": "47.2.0",
  "name": "Shouchan ModPack",
  "description": "Shouchanサーバー用ModPack"
}
```

**`/srv/http/shouchan-api/news.json`**
```json
[
  {
    "id": 1,
    "title": "最新情報",
    "content": "Shouchan Launcherへようこそ！",
    "date": "2026-04-10T00:00:00.000Z"
  }
]
```

**`/srv/http/shouchan-api/modpack-files.json`**（ModPackが決まってから記入）
```json
{
  "version": "v1.0.0",
  "files": [
    {
      "path": "mods/example-mod.jar",
      "url": "https://srgk.ddns.net/modpack/files/download/mods/example-mod.jar",
      "hash": "sha256ハッシュ値"
    }
  ]
}
```

ハッシュの計算方法:
```bash
sha256sum /srv/http/shouchan-api/modpack/mods/example-mod.jar
```

---

### 6. systemdサービスとして登録する

```bash
sudo nano /etc/systemd/system/shouchan-api.service
```

内容:
```ini
[Unit]
Description=Shouchan API Server
After=network.target

[Service]
Type=simple
User=あなたのユーザー名
WorkingDirectory=/srv/http/shouchan-api
ExecStart=/usr/bin/node server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable shouchan-api
sudo systemctl start shouchan-api
sudo systemctl status shouchan-api
```

---

### 7. SSL証明書の取得（Let's Encrypt）

> ⚠️ 先にnginxのHTTP設定が動いていること（手順8）を確認してから実行する。

```bash
sudo certbot --nginx -d srgk.ddns.net
sudo systemctl enable certbot-renew.timer
sudo systemctl start certbot-renew.timer
```

---

### 8. nginxの設定

Arch Linuxでは `sites-available` がない場合があるので `/etc/nginx/conf.d/shouchan.conf` に作成する。

```bash
sudo nano /etc/nginx/conf.d/shouchan.conf
```

内容:
```nginx
server {
    listen 80;
    server_name srgk.ddns.net;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name srgk.ddns.net;

    ssl_certificate /etc/letsencrypt/live/srgk.ddns.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/srgk.ddns.net/privkey.pem;

    # ModPackファイルを静的配信（大容量ファイル対応）
    location /modpack/files/download/ {
        alias /srv/http/shouchan-api/modpack/;
        autoindex off;
    }

    # ファイルアップロード用にボディサイズを拡大
    client_max_body_size 500M;

    # APIプロキシ
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
    }
}
```

```bash
  sudo nginx -t
sudo systemctl reload nginx
```

---

### 9. ModPackファイルの配置

- [ ] 使用するModとForgeバージョンを決定する
- [ ] `/srv/http/shouchan-api/modpack/mods/` にModの `.jar` ファイルを配置する
- [ ] `/srv/http/shouchan-api/modpack/config/` にconfigファイルを配置する（あれば）
- [ ] `modpack-files.json` に各ファイルのパス・URL・ハッシュを記入する
- [ ] `modpack-info.json` のバージョンを更新する

---

## 💻 ランチャー側で配布前にやること

### 1. 秘密情報の変更（**必須**）

`src/main/index.ts` の以下2箇所を変更する:

```ts
// 開発者登録コード（知っている人だけ開発者になれる）
const DEVELOPER_CODE = 'SHOUCHAN_DEV_2024'  // ← 任意の文字列に変更

// 開発者APIトークン（server.js の ADMIN_TOKEN と完全一致させる）
const DEV_ADMIN_TOKEN = 'shouchan-admin-secret'  // ← 任意の文字列に変更
```

> ⚠️ ソースコードを公開する場合は**必ず変更**すること。

### 2. アイコンの追加

- [ ] `resources/icon.png`（256×256 以上）を追加する
- [ ] `resources/icon.ico`（Windows用）を追加する

### 3. 動作確認

- [ ] `npm run dev` でランチャーを起動する
- [ ] 初期設定ウィザードが表示されることを確認する
- [ ] Microsoftログインが通ることを確認する
- [ ] サーバーステータスが「オンライン」と表示されることを確認する
- [ ] ModPack更新が正常に動作することを確認する
- [ ] `LAUNCH GAME` でMinecraftが起動することを確認する
- [ ] 開発者メニューでファイルのアップロード・削除ができることを確認する

### 4. ビルド（配布用インストーラー作成）

```bash
npm run package
```

- [ ] `dist/` フォルダに `.exe` インストーラーが生成されることを確認する
- [ ] 別のPCでインストールして動作テストをする
