# Shouchan Launcher - TODO

## 📌 プロジェクト情報

| 項目 | 内容 |
|---|---|
| サーバーOS | Arch Linux |
| Webサーバー | nginx |
| ドメイン | mc-shouchan.jp |
| ファイアウォール | UFW |
| APIサーバー場所 | `/srv/http/shouchan-api` |
| APIポート（内部） | 3000 |

---

## ✅ 完了済み（ランチャー側）

- [x] Electron + React + TypeScript + TailwindCSS プロジェクト構成
- [x] 初期設定ウィザード（Shouchanアカウント作成・Microsoft認証・Java・メモリ設定）
- [x] Shouchanアカウントシステム（サーバー管理・JWT認証）
- [x] Microsoft認証（msmc v5）／オフライン認証（開発者のみ）
- [x] 全画面にウィンドウコントロール（最小化・閉じる）
- [x] 複数ModPack対応
  - 開発者メニューでサーバー上にModPackを作成・管理（Forge / NeoForge / Fabric / Quilt / Vanilla対応）
  - プレイヤーはホーム画面でModPackを選択して起動（ModPackごとに別ゲームディレクトリ）
- [x] ModPack更新確認・未更新時の起動ブロック
- [x] 開発者メニュー（4タブ）
  - **ModPack管理**：一覧・作成・削除・一括アップロード
  - **MCアカウント**：Microsoft / オフライン切り替え
  - **ファイル管理**：ModPackを選択して個別ファイルのアップロード・削除
  - **ニュース**：記事の追加・編集・削除
- [x] Java自動インストール（Adoptium JRE 17）
- [x] 設定変更時にサーバーへ自動同期
- [x] Minecraft assets（背景・言語・フォント）不足時の自動取得と assetIndex 自動解決
- [x] **ランチャー自動アップデート機能**（electron-updater）
  - 起動時に自動チェック、1時間ごとに定期的チェック
  - ダウンロード進行状況表示
  - ユーザー確認後に再起動して更新適用
  - 設定画面から手動チェックも可能

---

## 📋 未定事項（決まり次第記入）

- [ ] 使用するMinecraftバージョン: `______`
- [ ] 使用するModLoader: Forge / NeoForge / Fabric / Quilt / Vanilla → `______`
- [ ] ModLoaderバージョン: `______`
- [ ] 導入するMod一覧: （別途リストアップ）
- [ ] MinecraftサーバーのIPアドレス/ポート: `______`

---

## 🔐 メール認証 / ログイン2FA で「あなたがやること」

> ランチャー側は実装済み（`/account/register/start|verify`, `/account/login/start|verify` を呼びます）。
> 以下は **サーバー側（mc-shouchan.jp）で必須** の作業です。

### 1) メール送信基盤を準備する

- [x] メールサーバー基盤あり（docker-mailserver v15.1.0 / `mail.mc-shouchan.jp`）
- [ ] SMTP接続方式を統一（推奨: `587 + STARTTLS`）
- [ ] 送信元を `Shouchan Launcher <shouchan@mc-shouchan.jp>` に固定
- [ ] SPF / DKIM / DMARC を本番DNSに反映済みか再確認
- [ ] APIサーバーにSMTP環境変数を設定（平文ハードコード禁止）
  - `MAIL_HOST=mail.mc-shouchan.jp`
  - `MAIL_PORT=587`
  - `MAIL_SECURE=false`
  - `MAIL_USER=shouchan@mc-shouchan.jp`
  - `MAIL_PASS=＜メールアカウントのパスワード＞`
  - `MAIL_FROM="Shouchan Launcher <shouchan@mc-shouchan.jp>"`

### 2) OTPセッション保存を追加する

- [ ] `otp_sessions.json`（またはDBテーブル）を用意
- [ ] `pendingToken`, `email`, `purpose(register|login)`, `codeHash`, `expiresAt`, `attempts` を保存
- [ ] OTP有効期限を 5 分に設定
- [ ] 検証失敗 5 回で無効化
- [ ] 検証成功時に即削除（再利用不可）

### 3) 新APIを実装する（必須）

- [ ] `POST /account/register/start`
  - 入力: `username`, `email`, `password`
  - 処理: メール重複チェック → OTP発行・送信 → `pendingToken` を返す
- [ ] `POST /account/register/verify`
  - 入力: `pendingToken`, `code`
  - 処理: OTP検証 → ユーザー作成 → JWT返却
- [ ] `POST /account/login/start`
  - 入力: `email`, `password`
  - 処理: パスワード照合 → OTP発行・送信 → `pendingToken` を返す
- [ ] `POST /account/login/verify`
  - 入力: `pendingToken`, `code`
  - 処理: OTP検証 → JWT返却（既存 `settings` も返却可）

### 4) セキュリティ対策を入れる

- [ ] メールアドレスを常に `trim().toLowerCase()` で正規化
- [ ] OTPコードは平文保存せずハッシュ化して保存
- [ ] `/start` エンドポイントにレート制限（IPとemail両方）
- [ ] 監査ログ（送信失敗、連続失敗、期限切れ）を残す

### 5) 本番反映と動作確認

- [ ] API再起動（pm2 / systemd など）
- [ ] nginx 経由で 4 エンドポイントが 200/4xx を正しく返すか確認
- [ ] ランチャーで「新規登録→コード入力→完了」を確認
- [ ] ランチャーで「ログイン→コード入力→完了（2FA必須）」を確認

### 6) 互換性メモ

- [ ] 旧 `/account/register` と `/account/login` 直ログインAPIを残す場合でも、ランチャー利用者には新4エンドポイントを必ず有効化
- [ ] 旧APIを完全停止するなら、他クライアント影響の有無を事前確認

---

## ✅ 何をすればいいか（コピペ実行手順）

> ここだけ順番どおり実行すればOKです。`<>` の中だけ自分の値に置き換えてください。

### 0) サーバーにSSH接続

```bash
ssh <あなたのユーザー名>@mc-shouchan.jp
```

### 1) APIディレクトリ準備と依存インストール

```bash
sudo mkdir -p /srv/http/shouchan-api
sudo chown $USER:$USER /srv/http/shouchan-api
cd /srv/http/shouchan-api

if [ ! -f package.json ]; then
  npm init -y
fi

npm install express cors multer bcrypt jsonwebtoken nodemailer express-rate-limit
```

### 2) 必須ファイル作成

```bash
cd /srv/http/shouchan-api
touch server.js
printf '[]\n' > accounts.json
printf '[]\n' > modpacks.json
printf '[{"id":1,"title":"最新情報","content":"Shouchan Launcherへようこそ！","date":"2026-04-10T00:00:00.000Z"}]\n' > news.json
printf '[]\n' > otp_sessions.json
```

- [ ] `server.js` にはこの `TODO.md` の「手順4」のコードをそのまま貼る

### 3) SMTP環境変数を作成（最重要）

```bash
cat > /srv/http/shouchan-api/.env << 'EOF'
MAIL_HOST=mail.mc-shouchan.jp
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=shouchan@mc-shouchan.jp
MAIL_PASS=<メールアカウントのパスワード>
MAIL_FROM=Shouchan Launcher <shouchan@mc-shouchan.jp>
JWT_SECRET=<ランダムで長い秘密文字列>
ADMIN_TOKEN=<src/main/index.ts の DEV_ADMIN_TOKEN と同じ値>
EOF
```

権限を絞る:

```bash
chmod 600 /srv/http/shouchan-api/.env
```

### 4) systemdサービス設定（.envを読み込む）

```bash
sudo tee /etc/systemd/system/shouchan-api.service > /dev/null << 'EOF'
[Unit]
Description=Shouchan API Server
After=network.target

[Service]
Type=simple
User=<あなたのユーザー名>
WorkingDirectory=/srv/http/shouchan-api
EnvironmentFile=/srv/http/shouchan-api/.env
ExecStart=/usr/bin/node server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF
```

反映:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now shouchan-api
sudo systemctl restart shouchan-api
sudo systemctl status shouchan-api --no-pager
```

### 5) nginx設定と反映

- [ ] この `TODO.md` の「手順8 nginx設定」を `/etc/nginx/conf.d/shouchan.conf` に配置

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 6) SSL証明書（未取得の場合）

```bash
sudo certbot --nginx -d mc-shouchan.jp
sudo systemctl enable --now certbot-renew.timer
```

### 7) API疎通確認（必須）

```bash
curl -i https://mc-shouchan.jp/status
curl -i https://mc-shouchan.jp/modpack/list
```

`register/start` 動作確認:

```bash
curl -i -X POST https://mc-shouchan.jp/account/register/start \
  -H 'Content-Type: application/json' \
  -d '{"username":"testuser","email":"<テスト用メールアドレス>","password":"password123"}'
```

`login/start` 動作確認（テストユーザー作成後）:

```bash
curl -i -X POST https://mc-shouchan.jp/account/login/start \
  -H 'Content-Type: application/json' \
  -d '{"email":"<テスト用メールアドレス>","password":"password123"}'
```

### 8) ランチャー側 最終確認（あなたが実施）

- [ ] 新規登録でメールにコードが届く
- [ ] 間違ったコードでは登録完了しない
- [ ] 正しいコードで登録完了する
- [ ] ログインでも必ずコード入力が要求される
- [ ] コード5回ミスで失効する
- [ ] 5分経過したコードで失敗する

### 9) 開発者権限付与（必要時）

```bash
node -e "
const fs = require('fs');
const file = '/srv/http/shouchan-api/accounts.json';
const users = JSON.parse(fs.readFileSync(file));
const target = '<対象メールアドレス>'.trim().toLowerCase();
const u = users.find(u => String(u.email || '').trim().toLowerCase() === target);
if (u) { u.role = 'developer'; fs.writeFileSync(file, JSON.stringify(users, null, 2)); console.log('Done:', u.username); }
else console.log('User not found');
"
```

### 10) 反映されない時の確認コマンド

```bash
sudo systemctl restart shouchan-api
sudo systemctl status shouchan-api --no-pager
sudo journalctl -u shouchan-api -n 200 --no-pager
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🖥️ サーバー側でやること

### 手順1: パッケージのインストール

```bash
sudo pacman -Syu
sudo pacman -S nodejs npm certbot certbot-nginx
```

### 手順2: ファイアウォールの設定

```bash
sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### 手順3: APIサーバーのセットアップ

```bash
sudo mkdir -p /srv/http/shouchan-api
sudo chown $USER:$USER /srv/http/shouchan-api
cd /srv/http/shouchan-api
npm init -y
npm install express cors multer bcrypt jsonwebtoken nodemailer express-rate-limit
```

### 手順4: `server.js` を作成する

> ⚠️ `ADMIN_TOKEN` は `src/main/index.ts` の `DEV_ADMIN_TOKEN` と**必ず同じ値**にすること。  
> ⚠️ `JWT_SECRET` は推測されにくいランダムな長い文字列に**必ず変更**すること。

```js
const express = require('express')
const cors    = require('cors')
const path    = require('path')
const fs      = require('fs')
const multer  = require('multer')
const bcrypt  = require('bcrypt')
const jwt     = require('jsonwebtoken')
const crypto  = require('crypto')
const nodemailer = require('nodemailer')

const app = express()
app.use(cors())
app.use(express.json())

// ===== 設定 =====
const BASE_DIR      = '/srv/http/shouchan-api'
const ADMIN_TOKEN   = 'shouchan-admin-secret-2026-0728' // ← DEV_ADMIN_TOKEN と合わせる
const JWT_SECRET    = 'your-jwt-secret-change-this'     // ← 必ず変更する
const MAIL_HOST     = process.env.MAIL_HOST || 'mail.mc-shouchan.jp'
const MAIL_PORT     = Number(process.env.MAIL_PORT || 587)
const MAIL_SECURE   = String(process.env.MAIL_SECURE || 'false') === 'true'
const MAIL_USER     = process.env.MAIL_USER || 'shouchan@mc-shouchan.jp'
const MAIL_PASS     = process.env.MAIL_PASS || ''
const MAIL_FROM     = process.env.MAIL_FROM || 'Shouchan Launcher <shouchan@mc-shouchan.jp>'
const USERS_FILE    = path.join(BASE_DIR, 'accounts.json')
const OTP_FILE      = path.join(BASE_DIR, 'otp_sessions.json')
const MODPACKS_FILE = path.join(BASE_DIR, 'modpacks.json')
const OTP_TTL_MS    = 5 * 60 * 1000

const mailTransporter = nodemailer.createTransport({
  host: MAIL_HOST,
  port: MAIL_PORT,
  secure: MAIL_SECURE,
  auth: {
    user: MAIL_USER,
    pass: MAIL_PASS
  },
  requireTLS: !MAIL_SECURE,
  tls: { minVersion: 'TLSv1.2' }
})

// ===== ヘルパー =====
function readJson(file, fallback) {
  if (!fs.existsSync(file)) return fallback
  return JSON.parse(fs.readFileSync(file, 'utf8'))
}
function writeJson(file, data) {
  fs.writeFileSync(file, JSON.stringify(data, null, 2))
}
function getUsers()       { return readJson(USERS_FILE, []) }
function saveUsers(u)     { writeJson(USERS_FILE, u) }
function getOtps()        { return readJson(OTP_FILE, []) }
function saveOtps(o)      { writeJson(OTP_FILE, o) }
function getModpacks()    { return readJson(MODPACKS_FILE, []) }
function saveModpacks(l)  { writeJson(MODPACKS_FILE, l) }
function modpackDir(id)   { return path.join(BASE_DIR, 'modpacks', id) }
function normalizeEmail(email) {
  return String(email || '').trim().toLowerCase()
}
function hashOtp(code) {
  return crypto.createHash('sha256').update(String(code)).digest('hex')
}
function makePendingToken() {
  return crypto.randomBytes(24).toString('hex')
}
function generateOtpCode() {
  return String(Math.floor(100000 + Math.random() * 900000))
}
function cleanupOtps() {
  const now = Date.now()
  saveOtps(getOtps().filter(o => o.expiresAt > now))
}
async function sendOtpMail(email, code, purpose) {
  const subject = purpose === 'register'
    ? '[Shouchan Launcher] メール認証コード（新規登録）'
    : '[Shouchan Launcher] ログイン認証コード'
  const text = [
    'Shouchan Launcher 認証コードのお知らせ',
    '',
    `認証コード: ${code}`,
    '有効期限: 5分',
    '',
    'このメールに心当たりがない場合は破棄してください。'
  ].join('\n')

  if (!MAIL_PASS) {
    throw new Error('MAIL_PASS が未設定です')
  }

  await mailTransporter.sendMail({
    from: MAIL_FROM,
    to: email,
    subject,
    text
  })
}
function walkDir(dir, base = '') {
  const result = []
  if (!fs.existsSync(dir)) return result
  for (const name of fs.readdirSync(dir)) {
    const full = path.join(dir, name)
    const rel  = base ? `${base}/${name}` : name
    if (fs.statSync(full).isDirectory()) result.push(...walkDir(full, rel))
    else result.push({ path: rel, size: fs.statSync(full).size })
  }
  return result
}

// ===== 認証ミドルウェア =====
function adminAuth(req, res, next) {
  const token = req.headers['authorization']?.replace('Bearer ', '')
  if (token !== ADMIN_TOKEN) return res.status(401).json({ error: 'Unauthorized' })
  next()
}
function userAuth(req, res, next) {
  try {
    const token = req.headers['authorization']?.replace('Bearer ', '')
    const decoded = jwt.verify(token, JWT_SECRET)
    req.user = getUsers().find(u => u.id === decoded.id)
    if (!req.user) return res.status(401).json({ error: 'Invalid token' })
    next()
  } catch { res.status(401).json({ error: 'Invalid token' }) }
}

// ===== 公開 API =====

app.get('/status', (req, res) => res.json({ status: 'ok' }))

app.get('/news', (req, res) =>
  res.json(readJson(path.join(BASE_DIR, 'news.json'), [])))

// ModPack 一覧
app.get('/modpack/list', (req, res) => res.json(getModpacks()))

// ModPack 情報
app.get('/modpack/:id/info', (req, res) => {
  const mp = getModpacks().find(m => m.id === req.params.id)
  if (!mp) return res.status(404).json({ error: 'Not found' })
  res.json(mp)
})

// ModPack ファイルマニフェスト
app.get('/modpack/:id/files', (req, res) => {
  const file = path.join(modpackDir(req.params.id), 'files.json')
  if (!fs.existsSync(file)) return res.status(404).json({ error: 'Manifest not found' })
  const data = JSON.parse(fs.readFileSync(file, 'utf8'))
  res.setHeader('x-modpack-version', data.version || '')
  res.json(data.files || [])
})

// ModPack ファイルダウンロード（静的）
app.use('/modpack/:id/files/download', (req, res, next) => {
  express.static(path.join(modpackDir(req.params.id), 'files'))(req, res, () => res.status(404).end())
})

// ===== アカウント API =====

app.post('/account/register/start', async (req, res) => {
  cleanupOtps()
  const { username, email, password } = req.body
  const normalizedEmail = normalizeEmail(email)
  if (!username || !normalizedEmail || !password) {
    return res.status(400).json({ error: '全項目を入力してください' })
  }
  const users = getUsers()
  if (users.find(u => normalizeEmail(u.email) === normalizedEmail)) {
    return res.status(400).json({ error: 'このメールアドレスは既に使用されています' })
  }

  const code = generateOtpCode()
  const pendingToken = makePendingToken()
  const passwordHash = await bcrypt.hash(password, 10)
  const otps = getOtps().filter(o => o.email !== normalizedEmail || o.purpose !== 'register')
  otps.push({
    pendingToken,
    purpose: 'register',
    email: normalizedEmail,
    username,
    passwordHash,
    codeHash: hashOtp(code),
    attempts: 0,
    expiresAt: Date.now() + OTP_TTL_MS
  })
  saveOtps(otps)
  await sendOtpMail(normalizedEmail, code, 'register')
  res.json({ success: true, pendingToken })
})

app.post('/account/register/verify', async (req, res) => {
  cleanupOtps()
  const { pendingToken, code } = req.body
  const otps = getOtps()
  const idx = otps.findIndex(o => o.pendingToken === pendingToken && o.purpose === 'register')
  if (idx === -1) return res.status(400).json({ error: '認証セッションが見つかりません' })
  const sess = otps[idx]
  if (sess.expiresAt <= Date.now()) {
    saveOtps(otps.filter((_, i) => i !== idx))
    return res.status(400).json({ error: '認証コードの有効期限が切れています' })
  }
  if (hashOtp(code) !== sess.codeHash) {
    sess.attempts += 1
    if (sess.attempts >= 5) {
      saveOtps(otps.filter((_, i) => i !== idx))
      return res.status(400).json({ error: '試行回数超過のため無効になりました' })
    }
    saveOtps(otps)
    return res.status(400).json({ error: '確認コードが正しくありません' })
  }

  const users = getUsers()
  if (users.find(u => normalizeEmail(u.email) === sess.email)) {
    saveOtps(otps.filter((_, i) => i !== idx))
    return res.status(400).json({ error: 'このメールアドレスは既に使用されています' })
  }
  const user = {
    id: Date.now().toString(),
    username: sess.username,
    email: sess.email,
    passwordHash: sess.passwordHash,
    role: 'player',
    createdAt: new Date().toISOString(),
    settings: {},
    emailVerified: true
  }
  users.push(user)
  saveUsers(users)
  saveOtps(otps.filter((_, i) => i !== idx))
  const token = jwt.sign({ id: user.id }, JWT_SECRET, { expiresIn: '30d' })
  res.json({ success: true, id: user.id, username: user.username, email: user.email, token, role: user.role, createdAt: user.createdAt })
})

app.post('/account/login/start', async (req, res) => {
  cleanupOtps()
  const { email, password } = req.body
  const normalizedEmail = normalizeEmail(email)
  const user = getUsers().find(u => normalizeEmail(u.email) === normalizedEmail)
  if (!user || !(await bcrypt.compare(password, user.passwordHash))) {
    return res.status(401).json({ error: 'メールアドレスまたはパスワードが間違っています' })
  }

  const code = generateOtpCode()
  const pendingToken = makePendingToken()
  const otps = getOtps().filter(o => o.email !== normalizedEmail || o.purpose !== 'login')
  otps.push({
    pendingToken,
    purpose: 'login',
    email: normalizedEmail,
    userId: user.id,
    codeHash: hashOtp(code),
    attempts: 0,
    expiresAt: Date.now() + OTP_TTL_MS
  })
  saveOtps(otps)
  await sendOtpMail(normalizedEmail, code, 'login')
  res.json({ success: true, pendingToken })
})

app.post('/account/login/verify', (req, res) => {
  cleanupOtps()
  const { pendingToken, code } = req.body
  const otps = getOtps()
  const idx = otps.findIndex(o => o.pendingToken === pendingToken && o.purpose === 'login')
  if (idx === -1) return res.status(400).json({ error: '認証セッションが見つかりません' })
  const sess = otps[idx]
  if (sess.expiresAt <= Date.now()) {
    saveOtps(otps.filter((_, i) => i !== idx))
    return res.status(400).json({ error: '認証コードの有効期限が切れています' })
  }
  if (hashOtp(code) !== sess.codeHash) {
    sess.attempts += 1
    if (sess.attempts >= 5) {
      saveOtps(otps.filter((_, i) => i !== idx))
      return res.status(400).json({ error: '試行回数超過のため無効になりました' })
    }
    saveOtps(otps)
    return res.status(400).json({ error: '確認コードが正しくありません' })
  }

  const user = getUsers().find(u => u.id === sess.userId)
  if (!user) {
    saveOtps(otps.filter((_, i) => i !== idx))
    return res.status(401).json({ error: 'ユーザーが存在しません' })
  }
  saveOtps(otps.filter((_, i) => i !== idx))
  const token = jwt.sign({ id: user.id }, JWT_SECRET, { expiresIn: '30d' })
  res.json({
    success: true,
    id: user.id,
    username: user.username,
    email: user.email,
    token,
    role: user.role,
    createdAt: user.createdAt,
    settings: user.settings || {}
  })
})

app.get('/account/profile', userAuth, (req, res) => {
  const { passwordHash, ...safe } = req.user
  res.json(safe)
})

app.put('/account/settings', userAuth, (req, res) => {
  const users = getUsers()
  const idx = users.findIndex(u => u.id === req.user.id)
  users[idx].settings = { ...users[idx].settings, ...req.body.settings }
  saveUsers(users)
  res.json({ success: true })
})

app.get('/account/verify', userAuth, (req, res) =>
  res.json({ success: true, role: req.user.role }))

// ===== 管理者 API =====

// ModPack CRUD
app.get('/admin/modpacks', adminAuth, (req, res) => res.json(getModpacks()))

app.post('/admin/modpacks', adminAuth, (req, res) => {
  const { name, version, mcVersion, modLoader, loaderVersion, description } = req.body
  if (!name || !version || !mcVersion)
    return res.status(400).json({ error: 'name, version, mcVersion は必須です' })
  const id = name.toLowerCase().replace(/[^a-z0-9]+/g, '-') + '-' + Date.now()
  const mp = { id, name, version, mcVersion,
    modLoader: modLoader || undefined, loaderVersion: loaderVersion || undefined,
    description: description || undefined }
  const list = getModpacks()
  list.push(mp)
  saveModpacks(list)
  fs.mkdirSync(path.join(modpackDir(id), 'files'), { recursive: true })
  res.json({ success: true, id })
})

app.put('/admin/modpacks/:id', adminAuth, (req, res) => {
  const list = getModpacks()
  const idx = list.findIndex(m => m.id === req.params.id)
  if (idx === -1) return res.status(404).json({ error: 'Not found' })
  list[idx] = { ...list[idx], ...req.body }
  saveModpacks(list)
  res.json({ success: true })
})

app.delete('/admin/modpacks/:id', adminAuth, (req, res) => {
  saveModpacks(getModpacks().filter(m => m.id !== req.params.id))
  const dir = modpackDir(req.params.id)
  if (fs.existsSync(dir)) fs.rmSync(dir, { recursive: true })
  res.json({ success: true })
})

// ModPack ファイル操作
const mpStorage = multer.diskStorage({
  destination: (req, file, cb) => {
    const dest = path.dirname(path.join(modpackDir(req.params.id), 'files', req.body.path || file.originalname))
    fs.mkdirSync(dest, { recursive: true })
    cb(null, dest)
  },
  filename: (req, file, cb) => cb(null, path.basename(req.body.path || file.originalname))
})
app.post('/admin/modpacks/:id/upload', adminAuth, multer({ storage: mpStorage }).single('file'),
  (req, res) => res.json({ success: true }))

app.get('/admin/modpacks/:id/files', adminAuth, (req, res) =>
  res.json(walkDir(path.join(modpackDir(req.params.id), 'files'))))

app.delete('/admin/modpacks/:id/files', adminAuth, (req, res) => {
  const fp = path.join(modpackDir(req.params.id), 'files', req.body.path)
  if (fs.existsSync(fp)) fs.unlinkSync(fp)
  res.json({ success: true })
})

// マニフェスト更新（一括アップロード後）
app.put('/admin/modpacks/:id/manifest', adminAuth, (req, res) => {
  const { version, files: fileList } = req.body
  const dir = modpackDir(req.params.id)
  fs.mkdirSync(dir, { recursive: true })
  writeJson(path.join(dir, 'files.json'), { version, files: fileList })
  const list = getModpacks()
  const idx = list.findIndex(m => m.id === req.params.id)
  if (idx !== -1) { list[idx].version = version; saveModpacks(list) }
  res.json({ success: true })
})

// ニュース
app.get('/admin/news', adminAuth, (req, res) =>
  res.json(readJson(path.join(BASE_DIR, 'news.json'), [])))

app.put('/admin/news', adminAuth, (req, res) => {
  writeJson(path.join(BASE_DIR, 'news.json'), req.body)
  res.json({ success: true })
})

app.listen(3000, () => console.log('Shouchan API running on port 3000'))
```

### 手順5: 初期JSONファイルを作成する

**`/srv/http/shouchan-api/modpacks.json`**
```json
[]
```

**`/srv/http/shouchan-api/news.json`**
```json
[{ "id": 1, "title": "最新情報", "content": "Shouchan Launcherへようこそ！", "date": "2026-04-10T00:00:00.000Z" }]
```

**`/srv/http/shouchan-api/otp_sessions.json`**
```json
[]
```

> ModPackはランチャーの開発者メニュー「ModPack管理」タブから作成します。  
> 作成するとサーバー上に `/srv/http/shouchan-api/modpacks/{id}/` が自動生成されます。

### 手順6: サーバーのディレクトリ構成（参考）

```
/srv/http/shouchan-api/
├── server.js
├── accounts.json       # ユーザーアカウント
├── otp_sessions.json   # OTPセッション
├── modpacks.json       # ModPack一覧
├── news.json           # ニュース記事
└── modpacks/
    └── {modpack-id}/
        ├── files.json  # マニフェスト（path, url, hash, version）
        └── files/      # 実Modファイル（mods/, config/ など）
```

### 手順7: systemdサービスとして登録する

```bash
sudo nano /etc/systemd/system/shouchan-api.service
```

```ini
[Unit]
Description=Shouchan API Server
After=network.target

[Service]
Type=simple
User=あなたのユーザー名
WorkingDirectory=/srv/http/shouchan-api
ExecStart=/usr/bin/node server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now shouchan-api
sudo systemctl status shouchan-api
```

### 手順8: nginxの設定

```bash
sudo nano /etc/nginx/conf.d/shouchan.conf
```

```nginx
server {
    listen 80;
    server_name mc-shouchan.jp;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name mc-shouchan.jp;

    ssl_certificate     /etc/letsencrypt/live/mc-shouchan.jp/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mc-shouchan.jp/privkey.pem;

    client_max_body_size 500M;

    location ~ ^/modpack/([^/]+)/files/download/(.*)$ {
        alias /srv/http/shouchan-api/modpacks/$1/files/$2;
        autoindex off;
    }

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
    }
}
```

```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 手順9: SSL証明書の取得（Let's Encrypt）

> ⚠️ nginx起動済みの状態（手順8完了後）で実行する。

```bash
sudo certbot --nginx -d mc-shouchan.jp
sudo systemctl enable --now certbot-renew.timer
```

---

## 🧪 反映後の最終チェック（あなたがやること）

### 1) API / nginx 再起動

```bash
sudo systemctl restart shouchan-api
sudo systemctl status shouchan-api
sudo nginx -t && sudo systemctl reload nginx
```

### 2) エンドポイント疎通確認

```bash
curl -i https://mc-shouchan.jp/status
curl -i -X POST https://mc-shouchan.jp/account/register/start -H 'Content-Type: application/json' -d '{"username":"test","email":"test@example.com","password":"password123"}'
curl -i -X POST https://mc-shouchan.jp/account/login/start -H 'Content-Type: application/json' -d '{"email":"test@example.com","password":"password123"}'
```

### 3) ランチャー実機確認（必須）

- [ ] 新規登録: 認証コード入力なしでは完了しない
- [ ] 新規登録: 正しい認証コードで完了できる
- [ ] ログイン: 認証コード入力なしでは完了しない
- [ ] ログイン: 正しい認証コードで完了できる
- [ ] 間違ったコード5回でセッション失効する
- [ ] 期限切れコードでログインできない

### 4) 開発者権限の付与方法

```bash
node -e "
const fs = require('fs');
const file = '/srv/http/shouchan-api/accounts.json';
const users = JSON.parse(fs.readFileSync(file));
const target = '対象のメールアドレス'.trim().toLowerCase();
const u = users.find(u => String(u.email || '').trim().toLowerCase() === target);
if (u) { u.role = 'developer'; fs.writeFileSync(file, JSON.stringify(users, null, 2)); console.log('Done:', u.username); }
else console.log('User not found');
"
```

### 5) 配布前チェック

- [ ] `src/main/index.ts` の `DEV_ADMIN_TOKEN` を変更し、サーバー `ADMIN_TOKEN` と一致させる
- [ ] サーバー `JWT_SECRET` をランダムな長い値に変更
- [ ] メール送信の環境変数を本番値に設定
- [ ] `npm run package` を実行して配布ビルドを作成
